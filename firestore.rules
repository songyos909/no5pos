rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // POS data is accessible only to the shop's Firebase administrator.
    match /{document=**} {
      allow read, write: if request.auth != null
        && request.auth.token.email == 'songyos909@gmail.com';
    }
  }
}
